plugins {
    id 'application'
}

application {
    mainClass = 'io.mikle.test.mds.starter.Application'
}

group 'io.mikle.test.mds'
version '1.0-SNAPSHOT'

dependencies {
    implementation project(':service-api')
    runtimeOnly project(':service-core')
}